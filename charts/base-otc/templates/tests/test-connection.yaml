apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "base-otc.fullname" . }}-test-connection"
  labels:
    {{- include "base-otc.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "base-otc.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
