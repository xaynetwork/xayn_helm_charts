apiVersion: v1
kind: Service
metadata:
  name: {{ include "vllm-otc.fullname" . }}
  labels:
    {{- include "vllm-otc.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "vllm-otc.selectorLabels" . | nindent 4 }}
